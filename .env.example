# Optional. Copy only each provider's verification token, not the full HTML tag.
# Configure these in Vercel's Production environment before rebuilding.
GOOGLE_SITE_VERIFICATION=
BING_SITE_VERIFICATION=
